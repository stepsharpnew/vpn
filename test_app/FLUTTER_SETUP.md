# Подробное руководство по установке Flutter SDK на Windows

Это руководство поможет вам установить Flutter SDK с нуля на Windows, даже если вы никогда не работали с Flutter.

---

## Шаг 1: Установка Flutter SDK

### 1.1. Скачивание Flutter SDK

1. Откройте браузер и перейдите на официальный сайт Flutter: https://docs.flutter.dev/get-started/install/windows

2. Нажмите на кнопку **"Download Flutter SDK"** или перейдите напрямую по ссылке: https://docs.flutter.dev/get-started/install/windows

3. Скачайте ZIP-архив с Flutter SDK (обычно файл называется `flutter_windows_XXX-stable.zip`, где XXX — версия)

4. **Важно:** НЕ распаковывайте архив в папки типа:
   - `C:\Program Files\` (требуются права администратора)
   - `C:\Windows\`
   - Папки с пробелами в пути
   - Папки, требующие специальных прав

5. **Рекомендуемое место:** Создайте папку `C:\src` (если её нет) и распакуйте туда архив

   **Как распаковать:**
   - Нажмите правой кнопкой мыши на скачанный ZIP-файл
   - Выберите "Извлечь все..." (Extract All)
   - В качестве папки назначения укажите `C:\src`
   - После распаковки у вас должна быть папка `C:\src\flutter`

### 1.2. Проверка установки

1. Откройте Проводник Windows и перейдите в `C:\src\flutter`
2. Убедитесь, что там есть файл `flutter.bat` в папке `bin`

---

## Шаг 2: Добавление Flutter в PATH

PATH — это переменная окружения, которая указывает Windows, где искать исполняемые программы. Добавив Flutter в PATH, вы сможете запускать команды `flutter` из любого места.

### 2.1. Через графический интерфейс (рекомендуется)

1. Нажмите клавишу **Windows** на клавиатуре
2. Введите "переменные среды" (или "environment variables")
3. Выберите **"Изменение переменных среды для вашей учетной записи"** (Edit environment variables for your account)

   **Альтернативный способ:**
   - Нажмите `Win + R`
   - Введите `sysdm.cpl` и нажмите Enter
   - Перейдите на вкладку "Дополнительно" (Advanced)
   - Нажмите кнопку "Переменные среды..." (Environment Variables)

4. В разделе **"Переменные пользователя"** (User variables) найдите переменную `Path`
5. Если переменной `Path` нет, создайте её:
   - Нажмите "Создать..." (New)
   - Имя переменной: `Path`
   - Значение переменной: `C:\src\flutter\bin`
6. Если переменная `Path` уже существует:
   - Выделите её и нажмите "Изменить..." (Edit)
   - Нажмите "Создать" (New)
   - Введите: `C:\src\flutter\bin`
   - Нажмите "ОК" во всех открытых окнах

### 2.2. Через PowerShell (альтернативный способ)

1. Откройте PowerShell **от имени администратора**:
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell (администратор)" или "Терминал (администратор)"

2. Выполните команду:
   ```powershell
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\src\flutter\bin", "User")
   ```

### 2.3. Проверка добавления в PATH

1. **Закройте все открытые терминалы/командные строки** (важно!)
2. Откройте новый терминал:
   - Нажмите `Win + R`
   - Введите `cmd` или `powershell` и нажмите Enter
3. Выполните команду:
   ```bash
   flutter --version
   ```
4. Если вы видите информацию о версии Flutter — отлично! Если видите ошибку "flutter не является внутренней или внешней командой", повторите шаги 2.1 или 2.2

---

## Шаг 3: Установка Android Studio

Android Studio — это официальная среда разработки для Android, которая включает в себя необходимые инструменты для разработки Flutter-приложений.

### 3.1. Скачивание Android Studio

1. Перейдите на официальный сайт: https://developer.android.com/studio
2. Нажмите кнопку **"Download Android Studio"**
3. Скачайте установщик (файл `.exe`, размер около 1 ГБ)

### 3.2. Установка Android Studio

1. Запустите скачанный установщик (может потребоваться запуск от имени администратора)
2. Следуйте мастеру установки:
   - Нажмите "Next" на экране приветствия
   - Выберите компоненты для установки (оставьте все по умолчанию, включая Android SDK, Android SDK Platform, Android Virtual Device)
   - Выберите путь установки (рекомендуется оставить по умолчанию: `C:\Program Files\Android\Android Studio`)
   - Нажмите "Install"
   - Дождитесь завершения установки (может занять 5-10 минут)
   - Нажмите "Next" и затем "Finish"

3. Android Studio автоматически запустится при первом запуске

### 3.3. Первый запуск Android Studio

1. При первом запуске вы увидите мастер настройки:
   - Выберите "Do not import settings" (Не импортировать настройки), если устанавливаете впервые
   - Нажмите "OK"
   
2. Настройка типа установки:
   - Выберите "Standard" (Стандартная установка)
   - Нажмите "Next"
   
3. Выбор темы:
   - Выберите "Darcula" (темная) или "IntelliJ" (светлая) — на ваш выбор
   - Нажмите "Next"
   
4. Загрузка компонентов:
   - Android Studio автоматически скачает Android SDK и необходимые компоненты
   - Это может занять 10-30 минут в зависимости от скорости интернета
   - Дождитесь завершения загрузки
   
5. Нажмите "Finish" после завершения загрузки

### 3.4. Установка плагина Flutter в Android Studio

1. После запуска Android Studio:
   - Нажмите "More Actions" → "Plugins" (или File → Settings → Plugins)
   - Во вкладке "Marketplace" введите "Flutter"
   - Нажмите "Install" на плагине Flutter
   - **Важно:** При установке Flutter плагина система предложит установить плагин Dart — согласитесь (установите его тоже)
   - После установки нажмите "Restart IDE" для перезапуска Android Studio

---

## Шаг 4: Установка Android SDK и настройка эмулятора

### 4.1. Установка Android SDK через Android Studio

1. В Android Studio откройте:
   - `File` → `Settings` (или `File` → `Settings` → `Appearance & Behavior` → `System Settings` → `Android SDK`)
   - Или нажмите на значок SDK Manager на панели инструментов

2. Во вкладке "SDK Platforms":
   - Установите флажок на **Android 13.0 (Tiramisu)** или **Android 12.0 (S)**
   - Убедитесь, что установлены "Android SDK Platform" и "Google APIs"

3. Перейдите на вкладку "SDK Tools":
   - Убедитесь, что установлены:
     - ✅ Android SDK Build-Tools
     - ✅ Android SDK Command-line Tools
     - ✅ Android SDK Platform-Tools
     - ✅ Android Emulator
     - ✅ Google Play services
     - ✅ Intel x86 Emulator Accelerator (HAXM installer) — если используете процессор Intel
   
4. Нажмите "Apply" и затем "OK"
5. Дождитесь установки всех компонентов

### 4.2. Создание эмулятора Android (AVD)

1. В Android Studio откройте:
   - `Tools` → `Device Manager` (или нажмите на значок Device Manager на панели инструментов)

2. Нажмите "Create Device" (Создать устройство)

3. Выберите категорию устройства:
   - Рекомендуется: **Phone** → **Pixel 5** или **Pixel 6**

4. Нажмите "Next"

5. Выберите системный образ:
   - Рекомендуется: **Tiramisu** (API Level 33) или **S** (API Level 31)
   - Если системный образ не скачан, нажмите "Download" рядом с ним
   - Дождитесь загрузки
   - Выберите скачанный образ и нажмите "Next"

6. Настройте AVD:
   - Имя (AVD Name): оставьте по умолчанию или введите своё (например, "Pixel_5_API_33")
   - Остальные настройки можно оставить по умолчанию
   - Нажмите "Finish"

7. Эмулятор появится в списке Device Manager

### 4.3. Запуск эмулятора

1. В Device Manager нажмите на кнопку "Play" (▶) рядом с созданным эмулятором
2. Дождитесь загрузки эмулятора (первый запуск может занять 1-2 минуты)
3. Эмулятор готов к использованию!

---

## Шаг 5: Проверка установки через flutter doctor

`flutter doctor` — это команда, которая проверяет, правильно ли настроена ваша среда разработки Flutter.

### 5.1. Запуск flutter doctor

1. Откройте новый терминал:
   - Нажмите `Win + R`
   - Введите `cmd` или `powershell` и нажмите Enter
   - Или откройте Android Studio Terminal

2. Выполните команду:
   ```bash
   flutter doctor
   ```

### 5.2. Анализ результатов flutter doctor

После выполнения команды вы увидите что-то вроде:

```
Doctor summary (to see all details, run flutter doctor -v):
[✓] Flutter (Channel stable, 3.x.x, on Microsoft Windows, locale ru-RU)
[✓] Windows Version (Installed version of Windows is version 10 or higher)
[✓] Android toolchain - develop for Android devices (Android SDK version 33.0.0)
[✓] Chrome - develop for the web
[✓] Visual Studio - develop Windows apps
[✓] Android Studio (version 2023.x)
[✓] VS Code (version 1.x.x)
[✓] Connected device (2 available)
[✓] Network resources
```

#### Что означает каждый пункт:

- ✅ **[✓] Flutter** — Flutter SDK установлен и работает
- ✅ **[✓] Windows Version** — версия Windows подходит
- ✅ **[✓] Android toolchain** — Android SDK установлен и настроен
- ⚠️ **[!] Android toolchain** (желтый восклицательный знак) — есть проблемы, но можно работать
- ❌ **[✗] Android toolchain** (красный крестик) — требуются исправления
- ✅ **[✓] Android Studio** — Android Studio установлен
- ✅ **[✓] Connected device** — доступны устройства (эмулятор или физическое устройство)

### 5.3. Исправление проблем (если есть)

#### Проблема: "Android licenses not accepted"

**Решение:**
```bash
flutter doctor --android-licenses
```
Нажимайте `y` (yes) на все вопросы о лицензиях.

#### Проблема: "cmdline-tools component is missing"

**Решение:**
1. Откройте Android Studio
2. `File` → `Settings` → `Appearance & Behavior` → `System Settings` → `Android SDK`
3. Вкладка "SDK Tools"
4. Установите "Android SDK Command-line Tools (latest)"
5. Нажмите "Apply"

#### Проблема: "Android SDK file not found"

**Решение:**
1. Откройте Android Studio
2. `File` → `Settings` → `Appearance & Behavior` → `System Settings` → `Android SDK`
3. Проверьте путь "Android SDK Location" (обычно `C:\Users\ВашеИмя\AppData\Local\Android\Sdk`)
4. Если путь другой, скопируйте его
5. Создайте переменную окружения `ANDROID_HOME`:
   - `Win + R` → `sysdm.cpl` → "Дополнительно" → "Переменные среды"
   - В "Переменные пользователя" нажмите "Создать"
   - Имя: `ANDROID_HOME`
   - Значение: путь к Android SDK (например, `C:\Users\ВашеИмя\AppData\Local\Android\Sdk`)
   - Нажмите "ОК"

#### Проблема: "No devices available"

**Решение:**
1. Запустите эмулятор Android (см. раздел 4.3)
2. Или подключите физическое Android-устройство через USB с включенной отладкой по USB

### 5.4. Подробная информация

Для более детальной информации выполните:
```bash
flutter doctor -v
```

Эта команда покажет подробную информацию о каждом компоненте и поможет найти проблемы.

---

## Дополнительные полезные команды

После успешной установки вы можете использовать следующие команды:

```bash
# Проверка версии Flutter
flutter --version

# Обновление Flutter
flutter upgrade

# Список доступных устройств
flutter devices

# Создание нового Flutter-проекта
flutter create my_app

# Запуск приложения
flutter run
```

---

## Следующие шаги

После успешной установки:

1. ✅ Убедитесь, что `flutter doctor` показывает все галочки (или почти все)
2. ✅ Создайте тестовый проект: `flutter create test_app`
3. ✅ Запустите его на эмуляторе: `cd test_app` → `flutter run`
4. ✅ Изучите официальную документацию: https://docs.flutter.dev/

---

## Полезные ссылки

- **Официальная документация Flutter:** https://docs.flutter.dev/
- **Android Studio:** https://developer.android.com/studio
- **Flutter для Windows:** https://docs.flutter.dev/get-started/install/windows
- **Сообщество Flutter:** https://flutter.dev/community

---

## Решение частых проблем

### Проблема: Команда flutter не найдена после добавления в PATH

**Решение:**
1. Убедитесь, что вы закрыли все терминалы после изменения PATH
2. Откройте новый терминал
3. Если не помогло, перезагрузите компьютер

### Проблема: Эмулятор работает очень медленно

**Решение:**
1. Убедитесь, что включена виртуализация в BIOS/UEFI
2. Установите HAXM (для Intel) или используйте Hyper-V (для Windows Pro)
3. Уменьшите разрешение эмулятора в настройках AVD

### Проблема: Не могу найти Android SDK Location

**Решение:**
1. Откройте Android Studio
2. `File` → `Settings` → `Appearance & Behavior` → `System Settings` → `Android SDK`
3. Вверху указан путь "Android SDK Location"
4. Скопируйте этот путь и используйте его для переменной `ANDROID_HOME`

---

**Удачи в разработке с Flutter! 🚀**
